{
  "name": "event-translator",
  "version": "1.0.0",
  "description": "Service that translates domain events to significant events",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "amqplib": "^0.10.3",
    "mariadb": "^3.2.3",
    "dotenv": "^16.4.1"
  }
} 